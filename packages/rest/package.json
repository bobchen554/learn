{
  "name": "rest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:es && npm run build:cjs",
    "build:es": "rm -rf ./lib && tsc",
    "build:cjs": "rm -rf ./libcjs && tsc --p ./tsconfig.cjs.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "bob_chen",
  "license": "ISC",
  "devDependencies": {
    "@types/query-string": "^6.3.0",
    "typescript": "^3.7.2"
  }
}
